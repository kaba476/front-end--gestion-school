# Copier en .env ou .env.production et remplir.
# En production (Vercel/Netlify), définir cette variable dans le tableau de bord.

# URL de l’API backend (avec /api si tes routes sont préfixées par /api)
# Dev : http://localhost:5000/api
# Prod : https://ton-api.onrender.com/api
VITE_API_URL=http://localhost:5000/api
